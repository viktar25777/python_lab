class Stationery:
    def __init__(self, title):
        self.title = title

def draw(self):
        self.draw = draw

a = "name"
def my_name(a):
    try:
        a = input("Выбирите инструмент отрисовки(ручка, карандаш, маркер): ")
    except valueerror:
        return
    return a

print(my_name(a))

class Pen(Stationery):
    def __init__(self, title):
        self.title = title

def draw(self):
        self.draw = draw

b = "name"
def my_name1(b):
    try:
        b = input("Ручкой мы ресуем основные линии и обозначения на чертеже: ")
    except valueerror:
        return
    return b

print(my_name1(b))

class Pencil(Stationery):
    def __init__(self, title):
        self.title = title

def draw(self):
        self.draw = draw

c = "name"
def my_name2(c):
    try:
        c = input("Карандашом наносим мелкие детали на чертеже: ")
    except valueerror:
        return
    return c

print(my_name2(c))

class Handle(Stationery):
    def __init__(self,title):
        self.title = title

def draw(self):
        self.draw = draw

d = "name"
def my_name3(d):
    try:
        d = input("Маркером мы наносим самые важные детали на чертеже: ")
    except valueerror:
        return
    return d

print(my_name3(d))










